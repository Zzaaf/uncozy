{
  "name": "doodle-jump",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start:prod --prefix server",
    "start:dev": "npm run start:dev --prefix server"
  },
  "engines": {
    "node": ">=24"
  }
}